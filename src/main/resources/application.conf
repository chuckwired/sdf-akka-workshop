akka {
  loggers  = [akka.event.slf4j.Slf4jLogger]
  loglevel = debug

  actor {
    debug {
      lifecycle = on
      unhandled = on
    }
  }
}

akka-workshop {
  session-tracker {
    session-timeout = 20 seconds
  }
}